---
title: CodeRabbit Agent Mode
tagline: Multi-agent PR pipeline, one writes, one critiques, one tests, one validates compliance. Multi-step gates produce ship-ready suggestions.
attribution: CodeRabbit
tier: tool
canonical_url: https://docs.coderabbit.ai/
requires: CodeRabbit account (free tier available; paid for private repos)
setup_command: |
  # 1. Install the CodeRabbit GitHub app:
  #    https://github.com/apps/coderabbitai (one-click on your repo)
  #
  # 2. Add `.coderabbit.yaml` at the repo root:
  #    reviews:
  #      auto_review: { enabled: true }
  #      profile: chill        # or "assertive"
  #      request_changes_workflow: true
  #    chat:
  #      auto_reply: true
  #
  # 3. Open a PR. CodeRabbit reviews it within ~60s.
  # 4. Reply `@coderabbitai resolve` to apply suggested fixes.
when_to_use: You want a multi-agent reviewer gating every merge, writer / critic / tester / compliance run before a human sees the PR.
when_not_to_use: Solo prototypes, review automation overhead exceeds the work.
tags: [coderabbit, code-review, multi-agent, ci]
sources:
  - title: CodeRabbit docs
    author: CodeRabbit
    url: https://docs.coderabbit.ai/
    year: 2025
  - title: How CodeRabbit's agentic code validation helps with code reviews
    author: CodeRabbit
    url: https://www.coderabbit.ai/blog/how-coderabbits-agentic-code-validation-helps-with-code-reviews
    year: 2025
related:
  patterns: [writer-reviewer-two-session, evaluator-optimizer]
  practices: [verify-everything]
  tools: [coderabbit]
  workflows: [split-and-merge-fanout]
---
